name: codedown-languages

on:
  pull_request:
  push:
    branches: [master]

jobs:
  test:
    name: test
    runs-on: self-hosted
    strategy:
      fail-fast: false

    steps:
    - name: Tests
      run: |
        cd tests
        stack run -- --print
