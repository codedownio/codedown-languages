#! /usr/bin/env nix-shell
#! nix-shell -i bash -p nix gnused

set -e

SCRIPTDIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$SCRIPTDIR/.."

nix flake update nixpkgs

dev-update-default-nix
