// Variable inspector for the Rust (evcxr) Jupyter kernel.
//
// evcxr can't be introspected from user code, so there's nothing to set up here.
// The listing is produced by evcxr's `:vars_json` command (a codedown addition to
// evcxr; see ~/tools/evcxr), which prints the bound variables and their types as
// JSON, matching the uniform variable-inspector contract.
